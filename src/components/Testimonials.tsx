import { Container, Row} from "react-bootstrap";

const ServicesSection = () => {
    return (
        <section className="py-5">
            <Container>
                <Row className="mb-4">Testimonials</Row>
            </Container>
        </section>
    )
};

export default ServicesSection;